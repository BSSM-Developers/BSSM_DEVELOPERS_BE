spring:
  application:
    name: bssm_dev

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: false  # dev에서만 true

jwt:
  accessExp: 3600
  refreshExp: 604800
  header: Authorization
  prefix: Bearer
